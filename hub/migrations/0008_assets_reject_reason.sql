-- 驳回的理由,给作者看的。
--
-- 在此之前 reject 只把 assets_status 翻成 'rejected' 就完事:理由既没地方存,
-- 端点也不收 —— 审核台的驳回按钮只有一句 confirm。作者那边因此只看得到
-- 「图片未通过」,不知道是内容不合适、文件坏了,还是尺寸不对,只能换一张再
-- 试,靠猜。而他原来那张图已经连同 pending 的行一起删掉了,连回头看一眼都
-- 不行。
--
-- 一列自由文本,英文。审核的人用英文写,作者原样看到英文 —— 不做「编码 +
-- 各语种映射」那一套:审核这件事只有一个人在做,给它配十四份译文是拿维护
-- 成本换一个没人受益的整齐,而且真正有用的那半句(具体哪张图、哪里不对)
-- 无论如何都只能是自由文本。
--
-- 存量的 rejected 行留 NULL。客户端对 NULL 退回原来那句笼统提示,与这次改版
-- 之前的行为完全一致。
--
-- 作者重新提交(configs.js 的 updateConfig)或者资产过审时清空 —— 否则一条
-- 早就照做修好的旧理由会一直挂在那儿。
ALTER TABLE configs ADD COLUMN assets_reject_reason TEXT;
